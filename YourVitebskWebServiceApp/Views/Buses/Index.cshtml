@model IEnumerable<YourVitebskWebServiceApp.Models.Bus>

<div class="mb-3">
    <h2>Автобусы</h2>
</div>

<div class="table-responsive">
    <table class="table table-striped align-middle">
        <tr>
            <th>ID</th>
            <th>Номер</th>
            <th><a class="btn btn-outline-dark float-end" asp-controller="Buses" asp-action="Create">Добавить</a></th>
        </tr>
    
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.BusId</td>
                <td>Автобус №@item.Number</td>
                <td>
                    <div class="btn-group float-end" role="group">
                        <a class="btn btn-outline-dark" asp-controller="Buses" asp-action="Edit" asp-route-id="@item.BusId">Изменить</a>
                        <a class="btn btn-outline-danger" asp-controller="Buses" asp-action="Delete" asp-route-id="@item.BusId">Удалить</a>
                    </div>
                </td>

            </tr>
        }
    </table>
</div>