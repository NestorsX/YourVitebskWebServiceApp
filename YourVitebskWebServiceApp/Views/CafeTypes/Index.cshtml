@model IEnumerable<YourVitebskWebServiceApp.Models.CafeType>

<div class="mb-3">
    <h2>Виды заведений</h2>
</div>

<div class="table-responsive">
    <table class="table table-striped align-middle">
        <tr>
            <th>ID</th>
            <th>Вид заведения</th>
            <th><a class="btn btn-outline-dark float-end" asp-controller="CafeTypes" asp-action="Create">Добавить</a></th>
        </tr>
    
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.CafeTypeId</td>
                <td>@item.Name</td>
                <td>
                    <div class="btn-group float-end" role="group">
                        <a class="btn btn-outline-dark" asp-controller="CafeTypes" asp-action="Edit" asp-route-id="@item.CafeTypeId">Изменить</a>
                        <a class="btn btn-outline-danger" asp-controller="CafeTypes" asp-action="Delete" asp-route-id="@item.CafeTypeId">Удалить</a>
                    </div>
                </td>
            </tr>
        }
    </table>
</div>